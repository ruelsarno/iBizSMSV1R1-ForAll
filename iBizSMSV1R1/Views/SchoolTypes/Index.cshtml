@model IEnumerable<iBizSMSV1R1.Models.SchoolType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutOtherEntry.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.schooltypes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.schooltypes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.recno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.recno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.recno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
